import React from 'react';

const HostModal = (props) => {

<div id="updateModal" className="modal">
  <div className="modal-content">
    <span className="close" onClick={this.handleSpan.bind(this)}>&times;</span>
    <img src={this.state.host.photoUrl}/>
    <h2>{this.state.host.username}</h2>
    <h4>{this.state.host.email}</h4>
    <h3>{this.state.host.quote}</h3>
    <h4>{this.state.host.description}</h4>
    <h4>{this.state.host.story}</h4>
  </div>
</div>
